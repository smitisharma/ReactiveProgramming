package com.self.model

object PartialFunction extends App{
  
  def adder (m:Int, n:Int) = m+n
  
  val addTwoFunction  = adder(2, _:Int) 
  
  println(addTwoFunction(5))
  
   val addTwoFunction  = adder(2, _:Int) 
  

}